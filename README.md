## Estructura del proyecto

El proyecto está organizado dentro de `src/` de la siguiente manera:

`assets/`: Contiene imágenes, iconos y recursos estáticos.
`components/`: Elementos de la interfaz que se usan en varias partes, como botones o inputs.
`pages/`: Páginas principales de la aplicación.
`api/`: Configuración de llamadas a APIs.
